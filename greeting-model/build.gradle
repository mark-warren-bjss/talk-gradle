//file:noinspection GradlePackageUpdate
plugins {
    id "java"
}

description = "Domain model for the greeting application"

dependencies {
    annotationProcessor "org.projectlombok:lombok:${lombokVersion}"
    compileOnly "org.projectlombok:lombok:${lombokVersion}"
}
