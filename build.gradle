plugins {
    id "java"
}

allprojects {
    repositories {
        mavenCentral()
    }
}

subprojects {
    group = 'org.demo.greeting'
    version = "0.0.1"

    java {
        toolchain {
            languageVersion = JavaLanguageVersion.of(11)
        }
    }
}
